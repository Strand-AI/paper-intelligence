{
  "name": "paper-intelligence-cloud",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute paper-intelligence --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute paper-intelligence --local --file=schema.sql"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.9.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
